# @package paths

wrk: ???

config: ${paths.wrk}/conf/
datasets: ${paths.wrk}/datasets/
output: ${paths.wrk}/outputs/
generated: ${paths.output}/version-${version}/${now:%Y-%m-%d}/${now:%H-%M-%S}/generated/
